# Legacy — not used

This Kotlin Android app targeted **Supabase** and is **retired**.

Use **`afterten-orders-expo/`** (Firebase Auth + Firestore + Cloud Functions) for outlet transfer orders.

Do not deploy or extend this project for new outlets.
