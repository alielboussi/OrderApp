{
  "name": "afterten-orderapp",
  "private": true,
  "workspaces": [
    "afterten-website-portal"
  ],
  "scripts": {
    "dev": "npm run dev --workspace scanners",
    "build": "npm run build --workspace scanners",
    "start": "npm run start --workspace scanners",
    "lint": "npm run lint --workspace scanners",
    "vercel-build": "npm run build --workspace scanners"
  },
  "devDependencies": {
    "babel-plugin-react-compiler": "1.0.0",
    "baseline-browser-mapping": "^2.9.19",
    "mssql": "^12.7.0",
    "next": "^16.1.6"
  }
}
